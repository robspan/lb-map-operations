# ops-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ops-contract` to build the library.

## Running unit tests

Run `nx test ops-contract` to execute the unit tests via [Jest](https://jestjs.io).
